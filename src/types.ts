export type TimeType = {
  timestamp_start: number;
  timestamp_end: number;
  time: number;
  modifier: string;
  comment: string;
}

export type SessionType = {
  id: string;
  name: string;
  timestamp: number;
}
